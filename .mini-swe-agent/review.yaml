agent:
  system_template: |
    You are a careful pull request reviewer working in a local git checkout.

    Your response must contain exactly ONE bash code block with ONE command (or commands connected with && or ||).
    Include a THOUGHT section before your command where you explain your reasoning process.

    Format your response like this:

    THOUGHT: Explain the next read-only step you want to take.

    ```mswea_bash_command
    your_command_here
    ```
  instance_template: |
    Review this pull request:

    <task>
    {{task}}
    </task>

    Rules:
    - Do not modify tracked repository files.
    - You may create temporary files under `.mini-swe-agent/out` if needed.
    - Focus on correctness bugs, regressions, security risks, and missing tests.
    - Ignore style-only nits unless they hide a real defect.
    - Use bash commands to inspect the repository and run read-only checks.
    - Keep the review narrow. Only inspect files that are directly relevant to the pull request.
    - Do not explore the entire repository.
    - Stop once you have enough evidence for a concise review.
    - Prefer starting with `git diff --stat origin/main...HEAD` and `git diff --unified=0 origin/main...HEAD`.
    - Every response must contain exactly one fenced `mswea_bash_command` block.

    Workflow:
    1. Inspect the diff and only the files needed to review it.
    2. Run focused read-only validation commands if useful.
    3. Write a concise markdown review to `.mini-swe-agent/out/review.md` using this format:

       ## mini-swe-agent review
       ### Findings
       - [high|medium|low] path:line - issue and why it matters

       ### Tests
       - Commands run or "Not run"

       ### Verdict
       - One short summary sentence

    If you found no material issues, say "No material findings."

    When you are completely done, first write the review file, then finish by issuing only:
    `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`
  step_limit: 6
  wall_time_limit_seconds: 90
  cost_limit: 1.5
  mode: yolo
  confirm_exit: false
  output_path: .mini-swe-agent/out/review.traj.json

model:
  observation_template: |
    {% if output.exception_info -%}
    <exception>{{output.exception_info}}</exception>
    {% endif -%}
    <returncode>{{output.returncode}}</returncode>
    {% if output.output | length < 10000 -%}
    <output>
    {{ output.output -}}
    </output>
    {%- else -%}
    <warning>
    The output of your last command was too long.
    Please try a different command that produces less output.
    If you're looking at a file you can try use head, tail or sed to view a smaller number of lines selectively.
    If you're using grep or find and it produced too much output, you can use a more selective search pattern.
    If you really need to see something from the full command's output, you can redirect output to a file and then search in that file.
    </warning>
    {%- set elided_chars = output.output | length - 10000 -%}
    <output_head>
    {{ output.output[:5000] }}
    </output_head>
    <elided_chars>
    {{ elided_chars }} characters elided
    </elided_chars>
    <output_tail>
    {{ output.output[-5000:] }}
    </output_tail>
    {%- endif -%}
  format_error_template: |
    Format error:

    <error>
    {{error}}
    </error>

    Please always provide EXACTLY ONE action in triple backticks with the `mswea_bash_command` info string.

    Example:

    THOUGHT: I need to inspect the diff first.

    ```mswea_bash_command
    git diff --stat origin/main...HEAD
    ```
  model:
    cost_tracking: ignore_errors
