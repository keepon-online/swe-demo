name: mini-swe-agent

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  review:
    if: >-
      ${{
        startsWith(github.event.comment.body, '/mini-swe') &&
        contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)
      }}
    runs-on: ubuntu-latest

    steps:
      - name: Load review context
        uses: actions/github-script@v7
        id: context
        with:
          script: |
            const {owner, repo} = context.repo;
            const issue = context.payload.issue;
            const {data: repoInfo} = await github.rest.repos.get({owner, repo});
            const defaultBranch = repoInfo.default_branch;
            const {data: branch} = await github.rest.repos.getBranch({owner, repo, branch: defaultBranch});

            if (issue.pull_request) {
              const {data: pr} = await github.rest.pulls.get({owner, repo, pull_number: issue.number});
              core.setOutput("mode", "pull_request");
              core.setOutput("head_sha", pr.head.sha);
              core.setOutput("head_ref", pr.head.ref);
              core.setOutput("head_repo", pr.head.repo.full_name);
              core.setOutput("base_repo", pr.base.repo.full_name);
              core.setOutput("base_sha", pr.base.sha);
              core.setOutput("base_ref", pr.base.ref);
              core.setOutput("checkout_ref", pr.head.sha);
              core.setOutput("title", pr.title);
              core.setOutput("body", pr.body || "");
            } else {
              core.setOutput("mode", "issue");
              core.setOutput("head_sha", branch.commit.sha);
              core.setOutput("head_ref", defaultBranch);
              core.setOutput("head_repo", `${owner}/${repo}`);
              core.setOutput("base_repo", `${owner}/${repo}`);
              core.setOutput("base_sha", branch.commit.sha);
              core.setOutput("base_ref", defaultBranch);
              core.setOutput("checkout_ref", defaultBranch);
              core.setOutput("title", issue.title);
              core.setOutput("body", issue.body || "");
            }

      - name: Skip fork pull requests
        if: steps.context.outputs.mode == 'pull_request' && steps.context.outputs.head_repo != steps.context.outputs.base_repo
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              ...context.repo,
              issue_number: context.issue.number,
              body: "mini-swe-agent 已跳过这个 PR，因为它来自 fork 仓库；当前工作流不会向 fork 代码暴露模型相关密钥。"
            });

      - name: Stop after fork guard
        if: steps.context.outputs.mode == 'pull_request' && steps.context.outputs.head_repo != steps.context.outputs.base_repo
        run: exit 0

      - name: Check required secrets
        id: secrets
        env:
          HAS_MODEL_NAME: ${{ secrets.MSWEA_MODEL_NAME != '' }}
          HAS_API_KEY: ${{ secrets.THIRD_PARTY_API_KEY != '' }}
          HAS_API_BASE: ${{ secrets.THIRD_PARTY_API_BASE != '' }}
        run: |
          if [ "$HAS_MODEL_NAME" = "true" ] && [ "$HAS_API_KEY" = "true" ] && [ "$HAS_API_BASE" = "true" ]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Report missing secrets
        if: steps.secrets.outputs.ready != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              ...context.repo,
              issue_number: context.issue.number,
              body: [
                "mini-swe-agent 无法启动，因为缺少一个或多个必需的 Secrets。",
                "",
                "必需的 Secrets：",
                "- `MSWEA_MODEL_NAME`",
                "- `THIRD_PARTY_API_KEY`",
                "- `THIRD_PARTY_API_BASE`"
              ].join("\n")
            });

      - name: Stop after missing secret check
        if: steps.secrets.outputs.ready != 'true'
        run: exit 0

      - name: Check out review target
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.context.outputs.checkout_ref }}
          fetch-depth: 0

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Prepare model environment
        env:
          MSWEA_MODEL_NAME: ${{ secrets.MSWEA_MODEL_NAME }}
          OPENAI_API_KEY: ${{ secrets.THIRD_PARTY_API_KEY }}
          THIRD_PARTY_API_BASE: ${{ secrets.THIRD_PARTY_API_BASE }}
        run: |
          mkdir -p .mini-swe-agent/out
          mkdir -p "$HOME/.config/mini-swe-agent"
          cat <<EOF > "$HOME/.config/mini-swe-agent/.env"
          MSWEA_CONFIGURED=true
          MSWEA_MODEL_NAME=$MSWEA_MODEL_NAME
          OPENAI_API_KEY=$OPENAI_API_KEY
          OPENAI_API_BASE=$THIRD_PARTY_API_BASE
          MSWEA_COST_TRACKING=ignore_errors
          EOF

      - name: Build review context
        run: |
          if [ "${{ steps.context.outputs.mode }}" = "pull_request" ]; then
            git diff --stat "${{ steps.context.outputs.base_sha }}...${{ steps.context.outputs.head_sha }}" > .mini-swe-agent/out/diff.stat
            git diff --name-only "${{ steps.context.outputs.base_sha }}...${{ steps.context.outputs.head_sha }}" > .mini-swe-agent/out/files.txt
            git diff --unified=1 "${{ steps.context.outputs.base_sha }}...${{ steps.context.outputs.head_sha }}" > .mini-swe-agent/out/diff.patch
          else
            printf "(issue mode: no git diff available against main)\n" > .mini-swe-agent/out/diff.stat
            find cmd internal -type f | sort > .mini-swe-agent/out/files.txt
            printf "(issue mode: analyze current main code snapshot)\n" > .mini-swe-agent/out/diff.patch
            while IFS= read -r file; do
              printf "\n--- FILE: %s ---\n" "$file" >> .mini-swe-agent/out/source_snapshot.txt
              sed -n '1,220p' "$file" >> .mini-swe-agent/out/source_snapshot.txt
            done < .mini-swe-agent/out/files.txt
          fi

      - name: Run one-shot review
        env:
          MSWEA_MODEL_NAME: ${{ secrets.MSWEA_MODEL_NAME }}
          OPENAI_API_KEY: ${{ secrets.THIRD_PARTY_API_KEY }}
          THIRD_PARTY_API_BASE: ${{ secrets.THIRD_PARTY_API_BASE }}
        run: |
          cat <<'PY' > .mini-swe-agent/out/review_once.py
          import os
          from pathlib import Path
          from litellm import completion

          base_ref = os.environ["BASE_REF"]
          title = os.environ["REVIEW_TITLE"]
          body = os.environ["REVIEW_BODY"]
          trigger_comment = os.environ["TRIGGER_COMMENT"]
          review_mode = os.environ["REVIEW_MODE"]
          diff_stat = Path(".mini-swe-agent/out/diff.stat").read_text()
          files = Path(".mini-swe-agent/out/files.txt").read_text()
          diff_patch = Path(".mini-swe-agent/out/diff.patch").read_text()
          source_snapshot_path = Path(".mini-swe-agent/out/source_snapshot.txt")
          source_snapshot = source_snapshot_path.read_text() if source_snapshot_path.exists() else ""

          if len(diff_patch) > 45000:
              diff_patch = diff_patch[:45000] + "\n\n[diff truncated by workflow]"
          if len(source_snapshot) > 45000:
              source_snapshot = source_snapshot[:45000] + "\n\n[source snapshot truncated by workflow]"

          system_prompt = (
              "你是一名资深代码审查工程师。"
              "只输出简洁的中文 Markdown。"
              "重点关注正确性缺陷、行为回归、安全风险和缺失测试。"
              "不要给出纯样式类建议。"
          )

          if review_mode == "pull_request":
              user_prompt = f"""请审查这个 Pull Request，并只返回中文 Markdown。

              PR 标题：
              {title}

              PR 描述：
              {body or "(empty)"}

              触发评论：
              {trigger_comment}

              基线分支：
              {base_ref}

              变更文件：
              {files or "(none)"}

              Diff 统计：
              {diff_stat or "(none)"}

              Unified diff：
              {diff_patch or "(none)"}

              输出格式：
              ## mini-swe-agent 审查
              ### 发现
              - [high|medium|low] path:line - 问题及影响

              ### 验证
              - 已运行的命令，或“未运行”

              ### 结论
              - 一句简短总结

              如果没有实质性问题，请输出：
              ## mini-swe-agent 审查
              ### 发现
              - 未发现实质性问题。

              ### 验证
              - 未运行

              ### 结论
              - 在当前审查范围内未发现实质性问题。
              """
          else:
              user_prompt = f"""请结合当前 main 分支代码分析这个 GitHub Issue，并只返回中文 Markdown。

              Issue 标题：
              {title}

              Issue 描述：
              {body or "(empty)"}

              触发评论：
              {trigger_comment}

              当前分支：
              {base_ref}

              仓库文件：
              {files or "(none)"}

              相关源码快照：
              {source_snapshot or "(none)"}

              输出格式：
              ## mini-swe-agent 审查
              ### 发现
              - [high|medium|low] path:line - 疑似根因或缺陷，以及影响

              ### 验证
              - 已运行的命令，或“未运行”

              ### 结论
              - 一句简短总结

              如果没有实质性问题，请输出：
              ## mini-swe-agent 审查
              ### 发现
              - 未发现实质性问题。

              ### 验证
              - 未运行

              ### 结论
              - 在当前代码快照范围内未发现实质性问题。
              """

          response = completion(
              model=os.environ["MSWEA_MODEL_NAME"],
              api_key=os.environ["OPENAI_API_KEY"],
              api_base=os.environ["THIRD_PARTY_API_BASE"],
              custom_llm_provider="openai",
              messages=[
                  {"role": "system", "content": system_prompt},
                  {"role": "user", "content": user_prompt},
              ],
              temperature=0,
          )

          content = response.choices[0].message.content or ""
          Path(".mini-swe-agent/out/review.md").write_text(content.strip())
          PY

          BASE_REF="${{ steps.context.outputs.base_ref }}" \
          REVIEW_TITLE="${{ steps.context.outputs.title }}" \
          REVIEW_BODY="${{ steps.context.outputs.body }}" \
          REVIEW_MODE="${{ steps.context.outputs.mode }}" \
          TRIGGER_COMMENT="${{ github.event.comment.body }}" \
          uv run --with litellm python .mini-swe-agent/out/review_once.py | tee .mini-swe-agent/out/console.log

      - name: Build comment body
        if: always()
        run: |
          python3 - <<'PY'
          from pathlib import Path

          console_path = Path(".mini-swe-agent/out/console.log")
          review_path = Path(".mini-swe-agent/out/review.md")
          comment_path = Path(".mini-swe-agent/out/comment.md")

          body = ""
          if review_path.exists():
              body = review_path.read_text().strip()

          if not body:
              tail = ""
              if console_path.exists():
                  tail = "\n".join(console_path.read_text().splitlines()[-40:])
              body = "## mini-swe-agent 审查\n\n本次执行未生成有效审查内容，请查看 workflow 日志获取详情。"
              if tail:
                  body += f"\n\n```text\n{tail[:4000]}\n```"

          if len(body) > 60000:
              body = body[:60000] + "\n\n_Truncated by workflow._"

          comment_path.write_text(body)
          PY

      - name: Reply on the issue or pull request
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require("fs");
            const body = fs.readFileSync(".mini-swe-agent/out/comment.md", "utf8");

            await github.rest.issues.createComment({
              ...context.repo,
              issue_number: context.issue.number,
              body
            });
