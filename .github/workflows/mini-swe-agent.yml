name: mini-swe-agent

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  review:
    if: >-
      ${{
        github.event.issue.pull_request &&
        startsWith(github.event.comment.body, '/mini-swe') &&
        contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)
      }}
    runs-on: ubuntu-latest

    steps:
      - name: Load pull request metadata
        uses: actions/github-script@v7
        id: pr
        with:
          script: |
            const {owner, repo} = context.repo;
            const pull_number = context.issue.number;
            const {data: pr} = await github.rest.pulls.get({owner, repo, pull_number});

            core.setOutput("head_sha", pr.head.sha);
            core.setOutput("head_ref", pr.head.ref);
            core.setOutput("head_repo", pr.head.repo.full_name);
            core.setOutput("base_repo", pr.base.repo.full_name);
            core.setOutput("base_sha", pr.base.sha);
            core.setOutput("base_ref", pr.base.ref);
            core.setOutput("title", pr.title);
            core.setOutput("body", pr.body || "");

      - name: Skip fork pull requests
        if: steps.pr.outputs.head_repo != steps.pr.outputs.base_repo
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              ...context.repo,
              issue_number: context.issue.number,
              body: "mini-swe-agent skipped this PR because it comes from a fork and the workflow does not expose model secrets to forked code."
            });

      - name: Stop after fork guard
        if: steps.pr.outputs.head_repo != steps.pr.outputs.base_repo
        run: exit 0

      - name: Check required secrets
        id: secrets
        env:
          HAS_MODEL_NAME: ${{ secrets.MSWEA_MODEL_NAME != '' }}
          HAS_API_KEY: ${{ secrets.THIRD_PARTY_API_KEY != '' }}
          HAS_API_BASE: ${{ secrets.THIRD_PARTY_API_BASE != '' }}
        run: |
          if [ "$HAS_MODEL_NAME" = "true" ] && [ "$HAS_API_KEY" = "true" ] && [ "$HAS_API_BASE" = "true" ]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Report missing secrets
        if: steps.secrets.outputs.ready != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              ...context.repo,
              issue_number: context.issue.number,
              body: [
                "mini-swe-agent could not start because one or more required secrets are missing.",
                "",
                "Required secrets:",
                "- `MSWEA_MODEL_NAME`",
                "- `THIRD_PARTY_API_KEY`",
                "- `THIRD_PARTY_API_BASE`"
              ].join("\n")
            });

      - name: Stop after missing secret check
        if: steps.secrets.outputs.ready != 'true'
        run: exit 0

      - name: Check out pull request head
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.pr.outputs.head_sha }}
          fetch-depth: 0

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Prepare mini-swe-agent global config
        env:
          MSWEA_MODEL_NAME: ${{ secrets.MSWEA_MODEL_NAME }}
          OPENAI_API_KEY: ${{ secrets.THIRD_PARTY_API_KEY }}
          THIRD_PARTY_API_BASE: ${{ secrets.THIRD_PARTY_API_BASE }}
        run: |
          mkdir -p "$HOME/.config/mini-swe-agent"
          cat <<EOF > "$HOME/.config/mini-swe-agent/.env"
          MSWEA_CONFIGURED=true
          MSWEA_MODEL_NAME=$MSWEA_MODEL_NAME
          OPENAI_API_KEY=$OPENAI_API_KEY
          OPENAI_API_BASE=$THIRD_PARTY_API_BASE
          MSWEA_COST_TRACKING=ignore_errors
          EOF

      - name: Build review context
        run: |
          mkdir -p .mini-swe-agent
          mkdir -p .mini-swe-agent/out
          git diff --stat "${{ steps.pr.outputs.base_sha }}...${{ steps.pr.outputs.head_sha }}" > .mini-swe-agent/out/diff.stat
          git diff --name-only "${{ steps.pr.outputs.base_sha }}...${{ steps.pr.outputs.head_sha }}" > .mini-swe-agent/out/files.txt
          git diff --unified=1 "${{ steps.pr.outputs.base_sha }}...${{ steps.pr.outputs.head_sha }}" > .mini-swe-agent/out/diff.patch

      - name: Run one-shot review
        env:
          MSWEA_MODEL_NAME: ${{ secrets.MSWEA_MODEL_NAME }}
          MSWEA_COST_TRACKING: ignore_errors
          OPENAI_API_KEY: ${{ secrets.THIRD_PARTY_API_KEY }}
          THIRD_PARTY_API_BASE: ${{ secrets.THIRD_PARTY_API_BASE }}
        run: |
          cat <<'PY' > .mini-swe-agent/out/review_once.py
          import os
          from pathlib import Path
          from litellm import completion

          base_ref = os.environ["BASE_REF"]
          title = os.environ["PR_TITLE"]
          body = os.environ["PR_BODY"]
          trigger_comment = os.environ["TRIGGER_COMMENT"]
          diff_stat = Path(".mini-swe-agent/out/diff.stat").read_text()
          files = Path(".mini-swe-agent/out/files.txt").read_text()
          diff_patch = Path(".mini-swe-agent/out/diff.patch").read_text()

          if len(diff_patch) > 45000:
              diff_patch = diff_patch[:45000] + "\n\n[diff truncated by workflow]"

          system_prompt = (
              "You are a senior pull request reviewer. "
              "Return only concise markdown. "
              "Focus on correctness bugs, regressions, security risks, and missing tests. "
              "Do not suggest style-only nits."
          )

          user_prompt = f"""Review this pull request and return markdown only.

          PR title:
          {title}

          PR body:
          {body or "(empty)"}

          Trigger comment:
          {trigger_comment}

          Base branch:
          {base_ref}

          Changed files:
          {files or "(none)"}

          Diff stat:
          {diff_stat or "(none)"}

          Unified diff:
          {diff_patch or "(none)"}

          Output format:
          ## mini-swe-agent review
          ### Findings
          - [high|medium|low] path:line - issue and why it matters

          ### Tests
          - Commands run or "Not run"

          ### Verdict
          - One short summary sentence

          If there are no material issues, write:
          ## mini-swe-agent review
          ### Findings
          - No material findings.

          ### Tests
          - Not run

          ### Verdict
          - No material issues found in the reviewed diff.
          """

          response = completion(
              model=os.environ["MSWEA_MODEL_NAME"],
              api_key=os.environ["OPENAI_API_KEY"],
              api_base=os.environ["THIRD_PARTY_API_BASE"],
              custom_llm_provider="openai",
              messages=[
                  {"role": "system", "content": system_prompt},
                  {"role": "user", "content": user_prompt},
              ],
              temperature=0,
          )

          content = response.choices[0].message.content or ""
          Path(".mini-swe-agent/out/review.md").write_text(content.strip())
          PY

          BASE_REF="${{ steps.pr.outputs.base_ref }}" \
          PR_TITLE="${{ steps.pr.outputs.title }}" \
          PR_BODY="${{ steps.pr.outputs.body }}" \
          TRIGGER_COMMENT="${{ github.event.comment.body }}" \
          uv run --with litellm python .mini-swe-agent/out/review_once.py | tee .mini-swe-agent/out/console.log

      - name: Build comment body
        if: always()
        run: |
          python3 - <<'PY'
          from pathlib import Path

          console_path = Path(".mini-swe-agent/out/console.log")
          review_path = Path(".mini-swe-agent/out/review.md")
          comment_path = Path(".mini-swe-agent/out/comment.md")

          body = ""

          if review_path.exists():
              body = review_path.read_text().strip()

          if not body:
              tail = ""
              if console_path.exists():
                  tail = "\n".join(console_path.read_text().splitlines()[-40:])
              body = "## mini-swe-agent review\n\nAgent finished without a review payload. Check the workflow logs for details."
              if tail:
                  body += f"\n\n```text\n{tail[:4000]}\n```"

          if len(body) > 60000:
              body = body[:60000] + "\n\n_Truncated by workflow._"

          comment_path.write_text(body)
          PY

      - name: Reply on the pull request
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require("fs");
            const body = fs.readFileSync(".mini-swe-agent/out/comment.md", "utf8");

            await github.rest.issues.createComment({
              ...context.repo,
              issue_number: context.issue.number,
              body
            });
